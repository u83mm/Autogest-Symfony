{% extends 'base.html.twig' %}

{% block title %}Familia{% endblock %}

{% block body %}
    <h1>Familia</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ familium.id }}</td>
            </tr>
            <tr>
                <th>Tipo_familia</th>
                <td>{{ familium.tipoFamilia }}</td>
            </tr>
            <tr>
                <th>Nombre_familia</th>
                <td>{{ familium.nombreFamilia }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('familia_index') }}">back to list</a>

    <a href="{{ path('familia_edit', {'id': familium.id}) }}">edit</a>

    {{ include('familia/_delete_form.html.twig') }}
{% endblock %}
