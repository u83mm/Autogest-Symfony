<form method="post" action="{{ path('pedido_call_center_delete', {'id': pedido_call_center.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pedido_call_center.id) }}">
    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_RECAMBIOS')  %}
    <button class="btn button delete">Eliminar</button>
    {% endif %}
</form>
