<form method="post" action="{{ path('tipo_estado_delete', {'id': tipo_estado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_estado.id) }}">
    <button class="btn">Delete</button>
</form>
