{% extends 'base.html.twig' %}

{% block title %}TipoEstado{% endblock %}

{% block body %}
    <h1>TipoEstado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tipo_estado.id }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tipo_estado.estado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tipo_estado_index') }}">back to list</a>

    <a href="{{ path('tipo_estado_edit', {'id': tipo_estado.id}) }}">edit</a>

    {{ include('tipo_estado/_delete_form.html.twig') }}
{% endblock %}
